# context-path 설정
server.servlet.context-path=/

# UTF-8 encode 설정
server.servlet.encoding.charset=UTF-8  
server.servlet.encoding.enabled=true  
server.servlet.encoding.force=true

# 에러 페이지 설정
server.error.path=/error

# port 설정
server.port=8081

# log level 설정
logging.level.root = info

# JSP 설정
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

# /error 경로 매핑
spring.mvc.throw-exception-if-no-handler-found=true
spring.mvc.static-path-pattern=/static/**
# DB 설정
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.datasource.url=jdbc:mariadb://imfreepass.com:3306/mapshop
spring.datasource.username=mapshop
spring.datasource.password=1234

#mybatis
mybatis.mapper-locations= classpath:mybatis/mapper/**/**.xml


# 로그 레벨 설정 (옵션)
#logging.level.net.sf.log4jdbc=DEBUG
# MyBatis 설정
#mybatis.type-aliases-package=com.spring.project.domain
#mybatis.mapper-locations=classpath:mapper/**/**.xml

# 멀티파트 요청을 처리하는 디렉터리 경로
spring.servlet.multipart.location=c:/upload
